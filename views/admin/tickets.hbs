<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-sm-6 col-md-4 col-md-offset-4 col-sm-offset-3">
                    <div id="success" class="alert alert-success" {{#if noMessage}}hidden{{/if}}>
                        {{successMsg}}
                    </div>
                    <div id="danger" class="alert alert-danger" {{#if noErrorMsg}}hidden{{/if}}>
                        {{errorMsg}}
                    </div>
                </div>
            </div>
            {{>search}}
            <div class="row">
                <div class="panel-body">
                    <div class="dataTable_wrapper">
                        <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                            <thead>
                                <tr>
                                    <th><h2>Date Sold</h2></th>
                                    <th><h2>Sold To</h2></th>
                                    <th><h2>Type</h2></th>
                                    <th><h2>Picked Up?</h2></th>
                                    <th><h2>Action</h2></th>
                                </tr>
                            </thead>
                            <tbody>
                                {{# each tickets }}
                                <tr class="odd gradeX">
                                    <form action="/edit" method="POST">
                                    <input type='hidden' id='id' name='productId' value='{{ this.date_sold}}'>
                                    <td><h3>{{this.user_name}}</h3></td>
                                    <td><h4>{{this.ticket_type}}</h4></td>
                                    <td><h4>{{this.picked_up}}</h4></td>
                                    <td class="center">
                                        <p class="text-center">
                                        <a href="/admin/edit-ticket/{{this._id}}" class="btn btn-primary" role="button">Edit</a>
                                        </p>
                                    </td>
                                    </form>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>